info:
  name: Echo
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:8080/echo
  body:
    type: json
    data: |-
      {
      "texto":"Olá mundo!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
