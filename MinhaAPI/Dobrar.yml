info:
  name: Dobrar
  type: http
  seq: 4

http:
  method: POST
  url: http://localhost:8080/dobrar
  body:
    type: json
    data: |-
      {
        "num": 5
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
