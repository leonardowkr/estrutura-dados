info:
  name: Criar anotação
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:8080/anotacoes
  body:
    type: json
    data: |-
      {
        "title": "Sou uma anotação",
        "description": "Sou o texto da descrição",
        "authorId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
